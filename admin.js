document.addEventListener('DOMContentLoaded', function() {
    // Fetch and update data if needed
    // Example:
    // document.getElementById('orders-count').textContent = fetchOrdersCount();
});
